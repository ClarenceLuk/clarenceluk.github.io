import React from 'react';
import { makeStyles } from '@material-ui/core';
const useStyles = makeStyles(theme => {
    console.log(theme)
})

const Weather = () => {
    
    return (
        <div>
            Weather!
        </div>
    )
}