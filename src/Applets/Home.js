import React from 'react'
import { makeStyles } from '@material-ui/core'

const useStyles = makeStyles((theme) => {
    return ({
        root: {

        }
    })
})

const Home = () => {
    return (
        <div>
            Home!!
        </div>
    )
}

export default Home